/**
 * Draws the objective zones (flag zones)
 */
export default function drawObjectiveZones() {
    // Draw the objective zones
    window.objectives.forEach(({ x, y, team, progress, size }, i) => {
        // TODO: Draw the objective zones
    })
}
